<style is:inline>
@font-face {
  font-family: Inter;
  src: url('/fonts/inter.woff2') format('woff2-variations');
  font-display: swap;
  font-weight: 300 900;
  font-style: normal;
}

html {
  font: 400 1em Inter, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
}

:root {
  --color-primary: #75f;
  --color-secondary: #2bf;
  --color-tertiary: #afc;
  --color-surface-primary: #000;
  --color-surface-secondary: #111;
  --color-text-primary: #fff;
  --gradient: var(--color-primary),var(--color-secondary),var(--color-tertiary);
  --global-dir: column wrap;
  --global-link-size: 1.125rem;
  --global-link-home-size: calc(var(--global-link-size) * 1.5);
}

@media (prefers-color-scheme: light) {
  :root {
    --color-surface-primary: #FFF;
    --color-surface-secondary: #F0F0F0;
    --color-text-primary: #000;
    --color-secondary: #1665ea;
    --color-tertiary: #75f;
  }
}

body,
h2,
p,
figure {
  padding: 0;
  margin: 0;
  border: 0;
}

html, body {
  block-size: 100%;
}

body :where(*, *::before) {
  box-sizing: border-box;
}

body {
  -webkit-text-size-adjust: 100%;
  text-size-adjust: 100%;
  touch-action: manipulation;
  -moz-osx-font-smoothing: grayscale;
  -webkit-font-smoothing: antialiased;
  -webkit-tap-highlight-color: transparent;
  background-color: var(--color-surface-primary);
  color: var(--color-text-primary);
}

a {
  font-weight: 700;
  color: inherit;
  white-space: nowrap;
  text-decoration: none;
}

p {
  font-size: 1.25rem;
  letter-spacing: 0.03em;
  line-height: 1.7;
  -webkit-hyphens: none;
  hyphens: none;
}

p a {
  color: var(--color-secondary);
}

p a:is(:hover, :focus-visible) {
  color: var(--color-tertiary);
  text-decoration: underline;
}

::selection {
  color: black;
  background-color: yellow;
}

[data-sr-only] {
  width: 1px;
  height: 1px;
  overflow: hidden;
  position: absolute;
}

[data-wide-layout],
[data-std-layout] {
  inline-size: 100%;
  padding-inline: 1rem;
}

[data-std-layout] {
  max-inline-size: 48rem;
  margin-inline: auto;
}

.Link {
  display: flex;
  min-block-size: 48px;
  min-inline-size: 48px;
  padding-inline: 1.5ch;
  justify-content: center;
  align-items: center;
  font-size: var(--global-link-size);
  border-radius: 4px;
  text-transform: lowercase;
}

@media (min-width: 48rem) {
  :root {
    --global-dir: row wrap;
    --global-link-size: 1.25rem;
  }

  [data-wide-layout] {
    padding-inline: 3rem;
  }
}
</style>
